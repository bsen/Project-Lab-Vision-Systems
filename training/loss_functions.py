import torch


def smoothL1(target, prediction, beta=1.0):
    """The smooth L1 regression loss.
    The parameters should be of size (B, H, W).
    Pixels where the target disparity is 0 are ignored."""
    l_list = []
    # TODO

def three_pixel_err(target, prediction):
    # TODO
    pass
